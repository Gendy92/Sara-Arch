{
  "name": "sara-arch",
  "version": "294.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "lint": "eslint js/ tests/",
    "lint:fix": "eslint js/ tests/ --fix",
    "audit:check": "npm audit --audit-level=moderate",
    "health": "npm run lint && npm test && npm audit --audit-level=moderate",
    "migrate": "node scripts/run-migrations.js"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.43.0",
    "eslint": "^10.7.0",
    "globals": "^15.0.0",
    "jsdom": "^24.0.0",
    "vitest": "^4.1.10"
  }
}
